import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent настраивает окружение и рендерит компонент
// нативно на iOS/Android, а также в веб-браузере.
registerRootComponent(App);
